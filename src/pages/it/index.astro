---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { useTranslations } from '../../i18n/utils';

const lang = 'it';
const t = useTranslations(lang);
---

<BaseLayout title={t('home.title')} lang={lang} currentPath="/">
  <section class="hero">
    <h1>Ruedi R. Renggli</h1>
    <p class="subtitle">Compositore & Musicista</p>
  </section>

  <section class="biography">
    <h2>{t('home.biography')}</h2>
    <div class="bio-content">
      <p>
        Ruedi R. Renggli è un compositore e musicista svizzero con una profonda passione
        per le forme di espressione musicale creative. Le sue composizioni combinano elementi
        tradizionali con paesaggi sonori moderni.
      </p>
      <p>
        Con anni di esperienza nella produzione musicale, Ruedi ha sviluppato uno stile
        unico che raggiunge ascoltatori di diverse culture e lingue.
      </p>
      <p>
        Le sue opere sono caratterizzate da profondità melodica e complessità armonica che
        sono sia emotivamente toccanti che intellettualmente stimolanti.
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    text-align: center;
    padding: 3rem 0;
    border-bottom: 1px solid #e0e0e0;
    margin-bottom: 3rem;
  }

  .hero h1 {
    font-size: 3rem;
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-size: 1.5rem;
    color: #666;
    font-weight: 300;
  }

  .biography {
    max-width: 800px;
    margin: 0 auto;
  }

  .bio-content {
    font-size: 1.1rem;
    line-height: 1.8;
    color: #555;
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2rem;
    }

    .subtitle {
      font-size: 1.2rem;
    }
  }
</style>
